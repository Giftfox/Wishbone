[gd_resource type="Shader" format=3 uid="uid://c0efriqk4b0wu"]

[resource]
code = "shader_type canvas_item;

uniform vec4 color;

void fragment() {
	vec4 base_color = texture(TEXTURE, UV);
	COLOR = vec4(base_color.r * color.r, base_color.g * color.g, base_color.b * color.b, color.a * base_color.a);
}
"
